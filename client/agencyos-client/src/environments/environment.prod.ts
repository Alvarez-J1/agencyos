// Production environment.
// Swapped in for the production build via angular.json fileReplacements.
//
// TODO: Replace the placeholder below with your deployed backend origin
// (scheme + host, NO trailing slash and NO /api suffix), e.g.
//   apiBaseUrl: 'https://api.your-domain.com'
// If the backend is served from the same origin as this frontend (reverse
// proxied under the same domain), set apiBaseUrl: '' to use same-origin requests.
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.agencyos.example.com'
};
